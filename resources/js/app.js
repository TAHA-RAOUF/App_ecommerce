// Importer des dépendances globales comme Alpine.js, si tu l'utilises

// Si tu utilises Alpine.js, voici comment l'initialiser
import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();
